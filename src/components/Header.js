import React from 'react';

import { Layout, HeaderText } from '../styles';

// ////////////////////////////////////////////////////////

const Header = () => (
  <Layout paddingBottom="2rem">
    <HeaderText>Virtualized Table</HeaderText>
  </Layout>
);

export default Header;
